/**
 * @todo Create a jest-merge based on babel-merge that uses deepmerge.
 * Note: This is fundamentally different from the "projects" option.
 *
 * const merge = require('jest-merge');
 */
const common = require('./conf/jest.config.js');

module.exports = common;
